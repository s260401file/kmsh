---
tags: [kmsh, 回報, 進度]
---
# 工作項目回報

> 狀態快照：**2026-07-16**。分四類：✅已完成、🔧待完成、❓待確認、📅預計完成。
> 明細來源：[[開發進度]]、[[待辦清單]]、[[後台總覽]]、[[資料庫Schema]]、[[即時更新-輪詢設計]]、[[HIS可用與缺漏分析]]、[[2026-06-16-第5次-資料介接與後台]]。

---

## ✅ 已完成（已上線 / 已驗證）

### 白板前端（React，四站 /w52 /icu /or /er）
- 巢狀路由＋分頁；病人註記 **SVG 形狀系統**（W52/ICU/ER，OR 維持中文字）。
- **ER 檢傷 1–5 → A/B/C**；**ICU 病況 → C/B/A**；**OR 刀房移除 OR-04（剩 7 間）**。
- **ER 病室動態分區** ＋ 右上「三班醫護人員」面板。

### 後台管理（/admin）
- 跑馬燈／佈告欄、值班人員／常用電話、避難圖（圖片＋設備＋緊急聯絡）。
- 皆具多單位切換、CRUD、啟用/停用、排序。

### 即時更新（本期）
- **白板免 F5 自動更新（定時輪詢）**：跑馬燈 30s、四板佈告欄 60s；共用 `usePolling`／`useBulletin`／`pollingConfig`；斷線保留舊內容。
- **公告顯示起迄時間**：`Text` 表加 `StartAt`/`EndAt`，後台可設顯示期間，白板只顯示時間內者。

### 部署 / 版控（本期）
- DB 遷移（`Text` 加 `StartAt`/`EndAt`）已對 `Whiteboard` 執行。
- API（:5100）與前端（:80）已部署 IIS 並驗證（含避難圖 uploads 保留）；程式碼已 push `main`。

### 病室動態全面接真實資料（四站，已部署上線）★本期
> 模式：院方 Board API ＋ 自建 overlay/主檔 ＋ 後台 CRUD；以病歷號疊合、`usePolling` 20s 免 F5。詳 [[開發進度]]。
- **W52 / ICU**：Board_bed（W52 / AICU 4F・CICU 3F）＋ `WardPatientExt` overlay；ICU **4F/3F 分樓層＋切換**。Board_bed 已帶 **負責醫師/轉入日期/診斷** → 直接顯示，後台移除這三欄。
- **ER**：Board_ER ＋ 自建 `ErBed` 主檔平面圖 ＋ overlay；檢傷 A/B/C；**不佔床病人**面板；總床數由頁首設定（預設 19）。
- **OR**：Board_OR ＋ 自建 `OrRoom` 主檔（7 房）＋ overlay；手術狀態依時間推導。

### 自建模組（已建表上線，schema v4 / v7–v13）★本期
- **UnitInfo**（v9/v10）：各站頁首主任/護理長、ER 總床數，後台可編輯。
- **ErOnCallDoctor**（v4）：各科值班醫師面板＋後台。
- **OrShiftStaff/OrShiftRoom/OrHandover**（v8）：OR 手術派班＋特殊交班。
- **WardExamConsult**（v11）：W52/ICU/ER 檢查/會診自建。
- **OrDailySurgery**（v12）：OR 當日手術快照，修正「完成刀消失→統計縮水」，已完成可查清單。
- **IcuAntibiotic**（v13）：ICU 抗生素自建，以病歷號掛載。
- **後台選單重構**：通用群組 ＋ W52/ICU/OR/ER 四站管理分類，依登入單位過濾。

### 介接（其餘待院方）
- 高榮 `VghksApiService` 端點已實作（床位/病人/過敏/未執行檢查會診/急作）——**為對接規格的預留殼**，民生實際資料多未開放（見 [[_院方API總覽]]）。
- 科別代碼/主治/診斷/預定出院：備份庫 **DB2_DUMP** 實測有值（[[欄位資料實況]]）。

### 知識庫
- Obsidian 文件（需求/資料來源/API/後台/技術/會議）整理與校正。

### 值班／照服員／聯絡電話／值班表版面 大批上線（★2026-07，schema v22–v40）
- **值班醫師排程（中央）**：`OnCallDept`＋`OnCallRoster`(v26)，ER 每月維護、全院共用；看板 `GET oncall-board` 取 Slot=值班。已補 呼吸治療科 DRT(v34)、大外科 `--`(v39)。
- **各站顯示值班醫師**：`UnitOnCallDept`(v35)，W52/ICU/ER 選科別＋順序引用當日值班；ER≤10 科（取代舊 `ErOnCallDoctor` v4）。
- **照服員**：主檔 `CareAide`(v36)＋各站 `UnitCareAide`(v37)；W52/ICU 前台動態、ER 醫師/照服員設定改主檔下拉。
- **顯示聯絡電話**：`ContactPhone`(v38)，後台維護＋W52/ICU 前台動態。
- **一床多位責任護理師**：W52/ICU/ER 多人可勾同床、前台逗號並列（`BedStaffAssignment` 放寬）。
- **夜/假護理師排程**：`NightNurseRoster`(v40)，ER 月曆維護（無科別、小夜/小夜貳組、姓名純文字）；看板「夜專師」取當日小夜。
- **W52/ICU 值班表版面**：橫向 3 欄（三班護理師＋緊急應變編組 5 組＋標題右「夜專師」｜值班醫療團隊｜照服員＋聯絡電話）；W52 含第 4 班 12:00–20:00、值班表移右上 7×6、統計移中間；ICU 移除 書記/傳送/專科師/透析師/行政總值/值班(陳蕙君)。
- **緊急應變編組**：前台已顯示（單一 `EmergencyGroup`，一人一組）；進階自動化**待議先勿改**。

### OR 進階（★2026-07）
- **溫溼度記錄** `OrRoomEnv`(v33)＋前台手術第 8 格；**各科協助業務** 圖片頁籤（`BoardImage` kind=assist,v32）。
- **手術清單匯出 xlsx**（`SimpleXlsx`，完整姓名需登入）；**檢視密碼鍵盤＋逾時**（`UnitInfo` v30/v31）。
- **逐台刀刷手/流動/麻醉** `OrSurgeryNurse`(v28/v29)＋後台「刷手/流動設定」（月曆）；科別 `ORCATGY`／診斷 `ORDIAG`；已完成僅依 `EndTime`、取消 `ORSTATUS=82` 排除。

### 帳號 / 資安（★2026-07）
- **帳號連動 AD LDS**：後台建立/更名/停用(不刪)/重設密碼、我的帳號自改密碼。
- **檢查/會診 roster 模式**＋完成後 **24h 自動移除**。

### 白板細節優化與文件對照（★本期 2026-07；已部署＋push `main`）
> commit `30f286f`（白板/後台/DB）、`889b469`（html_demo 文件）。
- **ER/ICU 病床卡加第 3 排「Dr {主治醫師}」**：白板直接看到負責醫師免點詳情（取自 Board_ER／Board_bed 負責醫師）。ICU 因既有 `card-row3` 死樣式衝突，改用 `card-row-dr`。
- **ER 急診手術室 OER01/OER02 等高**：改同格 flex 上下均分（OER01 上、OER02 下），不動其他床卡與列高（`or-stack` 包裹層）。
- **ER 到院／留觀時間**：由 Board_ER「傳入日期」帶入到院時間並推算留觀時長；詳情加顯「檢傷分類」院方原始值（如 `3` ＋ n級 n症）。
- **讀取 loading 動畫**：四板病室動態、ER 大量傷患、後台病人臨床補充於院方 API 讀取時顯示 `BoardLoading`。
- **後台病人臨床補充移除「科別」欄**（W52/ICU/ER，已由院方 API 帶入；OR 保留）。
- **檢查／會診改依最新執行時間排序**（依規格，不再手動排序）。
- **W52 護理人員 demo 種子**（排班/醫師/交班/照護團隊 30 天滾動），使畫面不空。

### API 對照文件（供與院方資訊單位核對）★本期 2026-07
- **html_demo/api-spec.html** 四板整併單頁（左側站別切換）；各端點標「來源」（資訊室=院方 API／自建）、「HIS欄位存在」（對應 81 表資料字典之 資料表.欄位）、實際輪詢頻率（census 20s／跑馬燈 30s／佈告欄 60s）。
- 新增 **各單位API狀況.xlsx**（4 頁籤：頁籤/來源/HIS欄位存在/說明）。
- 新增 **需求符合對照.html**（規格書逐項對照畫面＋左側快查選單與計數徽章）。

### 已移除（清理完成）
- ER 假病人 / 5 筆不佔床(demo) 區塊已隨 JWT 授權一併移除（見 [[待辦清單]] 2026-07-04）。
- 檢查/會診 mock 已移除、改 roster 模式（設定後 24h 顯示、完成後自動移除）。

---

## 🔧 待完成（可開發，多數不等院方）

### 後台自建模組（多數已上線；DDL 見 [[資料庫Schema]]）
- **P1** ✅：站別/護理長/主任（`UnitInfo`）、護理排班/主護勾床（我的病床，支援一床多人）、會診/值班醫師（中央 `OnCallRoster`＋各站 `UnitOnCallDept`）。
- **P2** ✅：三班護理師（W52/ICU/ER 泛用）、值班醫師排程、照服員（`CareAide`/`UnitCareAide`）、醫師查房表、OR 刷手/流動（`OrSurgeryNurse`）。
- **P3** ✅：護理交班、OR 特殊交班、照護團隊；🔧 病人註記覆蓋層仍以 `WardPatientExt` overlay 手動維護。

### 白板介面待辦（第5次會議；本期多已完成）
- **ICU**：✅3F/4F 分頁切換、✅主治/診斷 API 帶入、✅會診/值班醫師區（顯示值班醫師）、✅照服員/聯絡電話；🔧策略病人標示、醫師白/夜斜線。
- **ER**：✅會診/值班醫師區、✅照服員、✅值班表（值班醫療團隊/聯絡電話分欄）；🔧醫師白/夜班。
- **OR**：✅特殊交班、✅溫溼度、✅各科協助業務、✅逐台刀刷手/流動/麻醉、✅手術清單匯出；待院方是否能帶入實際時間。

### 即時更新第 2 步
- 版本戳聚合端點 `/api/Board/{unit}/versions` ＋ `useBoardVersions`（省頻寬）。
- 病室動態 / 急診檢傷接資料後納入同套輪詢。

---

## ❓ 待確認（多數待院方資訊單位 / 高榮）

- **★有欄位但空值／實測結果（院方 2026-06-22 逐表查詢已回）**：詳 [[欄位資料實況]]。重點：表名 `_4A0`、病歷號鍵名跨表不同（ETHISNUM/ORHISNUM/RSHISNUM）、`BIMBA.ERDISPAT` 不存在（25 表）；DNR/抗生素旗標/化療旗標/會診去向 空、OROETYPE/RSSTATUS 全同值、候床/排程/NPO 時間異常、過敏多 NIL；**確認可用**：床/科別/主治/診斷/檢傷/手術核心/藥囑。**待院方逐欄決議**（補資料/自建/不顯示）＋取得抗生素藥名清單。各 JSON 設計已標註實測更新。
- **自建 DB schema（14 表）欄位與範圍** 與院方資訊單位確認後再建表。
- 各病人註記 **`SourceMode` 預設** 逐項確認（民生哪些真的有）。
- **各項目輪詢時間** 與院方確認（可接受的資料延遲上限）→ [[即時更新-輪詢設計]]。
- 高榮 **正式機資料開放**；備份庫（84）**排程同步修復**。
- 高榮 **護理紀錄/評估系統是否開放 API**：管路（NG/CVC/導尿+換管日）、隔離、高危跌、運送等級 A/B/C、氧氣、約束。
- **會診** 是否開放；**禁治療 / 洗腎** 來源。
- 代碼/對應表：Board_ER `病患動向`/`類別`、MBD/AAD、床號 ↔ `AM.HLOC.HBED`。
- **測謀**註記定義；**ICU 3F 平面圖**（待 ICU 護理長提供）。

---

## 📅 預計完成（時程）

- **2026 年 7 月**：硬體安裝（各站 55 吋觸控＋主機＋無線鍵鼠）、ER/ICU 現場勘查電源與網路、佈線。
- **2026 年 9 月**：系統**驗收、付款**。
- **近期（持續）**：先上線可用資料（必要時以 51/52 病房補充）供熟悉；到場網路測試；拜訪聯醫（雷雷）釐清高榮實際支援欄位以界定開發範圍。

---
相關：[[00-總覽]] · [[待辦清單]] · [[開發進度]] · [[後台總覽]]
